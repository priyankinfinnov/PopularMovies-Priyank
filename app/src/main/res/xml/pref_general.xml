<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="@string/pref_sortOrder_key"
        android:title="@string/pref_title_sortOrder"
        android:defaultValue="mp"
        android:entries="@array/pref_sortOrder_list_titles"
        android:entryValues="@array/pref_sortOrder_list_values"
        android:negativeButtonText="Cancel"
        android:positiveButtonText="Save" />

</PreferenceScreen>
